---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: elkusers.monitoring.snappcload.io
spec:
  group: monitoring.snappcload.io
  names:
    kind: ElkUser
    listKind: ElkUserList
    plural: elkusers
    singular: elkuser
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ElkUser is the Schema for the elkusers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ElkUserSpec defines the desired state of ElkUser
            properties:
              password:
                description: Password of the Elk cluster user
                maxLength: 16
                minLength: 8
                type: string
              role:
                description: Role of the Elk cluster user
                enum:
                - Admin
                - Dev
                type: string
              username:
                description: Username of the Elk cluster user
                maxLength: 16
                type: string
            type: object
          status:
            description: ElkUserStatus defines the observed state of ElkUser
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
